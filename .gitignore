# Entornos virtuales y secretos
.venv/
venv/
__pycache__/
*.pyc
.env
.env.*
!.env.example


# Configuración local de Claude Code (personal, no compartida)
.claude/settings.local.json

# Datasets y checkpoints — no se commitean (ver CLAUDE.md)
*.pgn
*.pgn.zst
training/checkpoints/
training/dataset_tablero/

# Frontend (React/Vite)
frontend/node_modules/
frontend/dist/

# App móvil (Flutter)
app_movil/.dart_tool/
app_movil/.flutter-plugins
app_movil/.flutter-plugins-dependencies
app_movil/build/

# Binarios de terceros descargados localmente (ej. Stockfish, sin necesitar
# admin para instalarlo) — cada quien lo baja con su propio script, no se commitea
tools/

# Sistema
.DS_Store

# Caches de test
.pytest_cache/
